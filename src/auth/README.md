# auth/

Lógica de autenticación reutilizable entre módulos: hashing de contraseña
(`password.ts`), firma/verificación de JWT access+refresh (`jwt.ts`) y TOTP
para 2FA (`totp.ts`).

Vacío por ahora. Se llena en la
[Fase 2 (Auth)](../../Docs/plan/fases/fase-02-authentication.md) del plan de
backend (BE-024..034).
