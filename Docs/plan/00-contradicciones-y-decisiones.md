[← Índice del plan](README.md)  ·  [Siguiente: 1. Resumen de arquitectura propuesta](01-resumen-arquitectura.md)

---

# 0. Antes de empezar: contradicciones detectadas y cómo se resolvieron

El encargo de este plan pidió señalar cualquier contradicción entre el alcance, [PAYMYLOAN_DATABASE_DESIGN.md](../PAYMYLOAN_DATABASE_DESIGN.md), el código actual y las instrucciones de este propio encargo, en vez de resolverla en silencio. Se detectaron tres contradicciones de fondo; las tres se plantearon explícitamente y se resolvieron con el usuario antes de escribir este plan. Se documentan aquí para que quede registro de la decisión y su motivo — todo el resto del documento asume estas tres respuestas como definitivas.

| # | Contradicción | Alcance / DATABASE_DESIGN.md decían | Este plan decidió | Motivo |
|---|---|---|---|---|
| D0-1 | Modelo de roles y tenencia | `LoanParty`: Lender/Borrower/Viewer simétricos por préstamo, cualquiera de las dos partes crea el préstamo e invita a la otra, sin concepto de "tenant" administrado por un Admin | **Modelo jerárquico multi-tenant**: Admin crea/administra Prestamistas (tenants); el Prestamista crea unilateralmente el Contrato y a sus Deudores; el Deudor **acepta formalmente los términos** antes de que el contrato se active | Confirmado explícitamente: "que el prestamista cree el contrato y el deudor lo acepte con los términos" — es un híbrido, no el modelo simétrico del alcance ni un modelo puramente unilateral sin aceptación |
| D0-2 | Estrategia de IDs | `cuid()` generado por Prisma Client, en Owner, en `paymyloan-back` y en todo [PAYMYLOAN_DATABASE_DESIGN.md](../PAYMYLOAN_DATABASE_DESIGN.md) | **UUID v7 nativo de Postgres**, generado en la base de datos (`uuidv7()`), lo que requiere subir la imagen de Postgres de 16 a **18** | Confirmado explícitamente: actualizar Postgres en vez de generar el UUIDv7 en el service layer |
| D0-3 | Aceptación de términos | El alcance la trata como el núcleo del producto (aceptación bilateral con IP/timestamp); el encargo de este plan, leído literalmente, solo mencionaba que el Deudor "ve" su contrato | **Aceptación obligatoria y bloqueante**: el contrato no pasa a `ACTIVE` hasta que todos los deudores asociados acepten la versión vigente de términos, con registro inmutable (decisión, fecha, IP, user agent) y cobertura completa en `AuditLog` | Confirmado explícitamente: "la aceptación debe ser bilateral de los términos obligatoria y además... lo de la auditoría" |

**Consecuencia de D0-1 sobre [PAYMYLOAN_DATABASE_DESIGN.md](../PAYMYLOAN_DATABASE_DESIGN.md)**: ese documento sigue siendo la fuente más profunda de análisis de Owner y de lógica financiera (separación `ScheduledPayment`/`Transaction`, prelación de pagos, idempotencia de webhooks, reglas de auditoría) y se reutiliza casi íntegro en la sección [9](09-pagos.md) de este plan. Lo que **no** se reutiliza tal cual es su capa de identidad/autorización (`LoanParty`, `LoanInvitation`, `Property`+`LoanProperty` como N:M, `PlatformAdmin` separado): esa capa se rediseña en la sección [4](04-base-de-datos.md) para la jerarquía Admin→Prestamista→(Contrato, Deudor) que pide este encargo.

**Otras decisiones de arquitectura tomadas por este plan** (no eran contradicciones bloqueantes, pero requerían una postura explícita para poder diseñar; se listan también en la sección [15](15-riesgos-y-decisiones-pendientes.md) como pendientes de confirmar con Spencer si se quiere revisar):

| # | Decisión | Alternativa descartada | Motivo |
|---|---|---|---|
| A-1 | Sesión vía **JWT propio (access + refresh token)**, expuesto como endpoints REST | NextAuth v5 (como Owner) | `paymyloan-back` es un backend headless sin páginas (confirmado: cero `page.tsx` en el repo), consumido por `paymyloan` como proyecto **separado** (dominio/puerto distintos). NextAuth está diseñado para manejar su propia cookie de sesión dentro de la misma app Next.js; forzar esa cookie entre dos dominios distintos exige la misma complejidad de CORS/SameSite que un JWT propio, sin ganar nada a cambio. Se reutiliza sí el patrón de Owner de bcrypt + `@otplib` para TOTP. |
| A-2 | `Autopay` (cobro recurrente) **no se modela en este plan** | Incluir `PaymentMethod` + `Autopay` completos como en [PAYMYLOAN_DATABASE_DESIGN.md](../PAYMYLOAN_DATABASE_DESIGN.md) | El alcance lo deja como pregunta abierta (Q8: "¿autopay obligatorio, opcional, o no existe en el MVP?") y el encargo de este plan solo pide que el deudor "genere/registre" pagos — no pide cobro recurrente automático. Se deja `PaymentMethod` (referencia a Stripe) porque sí hace falta para pagos puntuales, pero `Autopay` queda fuera del backlog inicial — ver sección [15](15-riesgos-y-decisiones-pendientes.md). |
| A-3 | **Stripe Connect vs cuenta única** sigue sin resolverse | — | El alcance lo marca como "la decisión que bloquea todo lo demás" (Q1) y el encargo de este plan no lo menciona. Se diseña `LenderProfile.stripeConnectedAccountId` como campo opcional/nulo para no bloquear el resto del modelo, pero el módulo de Pagos (sección [9](09-pagos.md), fase 7 del backlog) no puede implementarse en producción hasta cerrar esta decisión — ver sección [15](15-riesgos-y-decisiones-pendientes.md). |
| A-4 | `Property` **no es una tabla** — la dirección vive embebida en `Contract` | `Property` + `LoanProperty` (N:M) de [PAYMYLOAN_DATABASE_DESIGN.md](../PAYMYLOAN_DATABASE_DESIGN.md) | Instrucción explícita del encargo: "la dirección de la casa debe almacenarse directamente como parte de la información del contrato". Se pierde la posibilidad de blanket loans (una hipoteca sobre varias propiedades) sin migración de schema — se documenta como trade-off consciente en la sección [4.3](04-base-de-datos.md#43-tablas). |
| A-5 | Eliminación **siempre lógica** (`deletedAt`/`status`), nunca `DELETE` físico, para toda tabla con relevancia financiera o legal | `DELETE` real | Mismo principio que ya aplica [PAYMYLOAN_DATABASE_DESIGN.md](../PAYMYLOAN_DATABASE_DESIGN.md) y que ya sigue el código actual de `paymyloan-back` (`softDeleteUser`) — un prestamista, deudor o contrato puede quedar referenciado desde transacciones, documentos o la bitácora. |

---

## Decisiones 2026-09-04 (ronda Fase 1)

Antes de tocar ningún ticket de la Fase 1 (BE-008 a BE-023), se revisó toda la documentación existente, el `schema.prisma` real, las migraciones aplicadas, y el nuevo contexto funcional entregado por Spencer junto con el [Product Board](../PML%20—%20PayMyLoan.ai%20Product%20Board.pdf) del 3 sep 2026. Esa revisión encontró, además, que [IMPLEMENTATION_PROGRESS.md](../IMPLEMENTATION_PROGRESS.md) ya traía deltas (`C-1`, `M-1`, `M-4`, `M-5`, `M-6`) que nunca se integraron de vuelta a este plan base — quedaban solo como anotaciones sueltas en el checklist de avance, contradiciendo en silencio a [04-base-de-datos.md](04-base-de-datos.md). Esta ronda corrige eso: cada decisión de abajo dice explícitamente **qué documento(s) quedaban en conflicto**, no solo qué se decidió.

Formato de cada fila: **Comportamiento actual** (qué decía el plan o el código hoy) → **Requisito nuevo** (qué pide el encargo de Spencer y/o el Product Board) → **Conflicto** (con qué documento específico) → **Decisión** → **Motivo** → **Impacto** (qué tickets/tablas cambian).

| # | Comportamiento actual | Requisito nuevo | Conflicto con | Decisión | Motivo |
|---|---|---|---|---|---|
| `D-P1-1` | `UserRole` = `ADMIN`/`LENDER`/`BORROWER` ([04](04-base-de-datos.md) v1) | Product Board identifica 6 tipos de participante: Borrower, Lender, Bookkeeper, CPA, Title Company, Insurance Company | [04-base-de-datos.md](04-base-de-datos.md) v1 | Se agregan `BOOKKEEPER` e `INSURANCE_COMPANY` al enum ahora. `CPA` y `Title Company` **no** entran al enum esta fase — quedan como decisión de producto pendiente (sección [15](15-riesgos-y-decisiones-pendientes.md)) | Bookkeeper e Insurance Company tienen una acción descrita que exige cuenta propia dentro de la plataforma (ver registros financieros del prestamista; quedar asociada a un contrato). CPA/Title Company, en la redacción del board, solo reciben documentos/exports — no está confirmado que necesiten login propio |
| `D-P1-2` | `User.deletedAt` es el único mecanismo — el propio comentario del `schema.prisma` dice "eliminar acá significa apagar el acceso, no borrar el registro", y el código (`softDeleteUser`) lo usa como si fuera un botón único de "Eliminar" | Admin puede **activar** y **desactivar** un usuario, explícitamente distinto de eliminación lógica | Comentario de diseño en `prisma/schema.prisma` (líneas 25–27) y uso actual de `deletedAt` en `users.service.ts` | Se agrega `User.isActive Boolean @default(true)`, reversible, operado por Admin. `deletedAt` se mantiene para eliminación lógica de verdad (evento más raro y más permanente) | El requisito pide explícitamente que activar/desactivar **no sea** eliminación lógica — el modelo anterior no tenía dónde representar eso sin sobrecargar `deletedAt` con dos significados distintos |
| `D-P1-3` | `LenderProfile` es 1:1 con `User` y **es** el tenant (`Contract.lenderId`, `AuditLog.lenderId` apuntan directo a `LenderProfile`) | "Un usuario LENDER podrá crear N empresas que serán las entidades que realmente prestan el dinero" | [04-base-de-datos.md](04-base-de-datos.md) v1 completo (todo el concepto de tenant), [01-resumen-arquitectura.md](01-resumen-arquitectura.md) (multi-tenancy en una frase), [07-autenticacion-y-autorizacion.md](07-autenticacion-y-autorizacion.md) §7.5 (JWT/`withTenantScope` asumían `session.lenderId` único) | Se introduce `LenderCompany` (nueva tabla, 1:N desde `LenderProfile`) como el tenant real. Toda tabla de negocio que apuntaba a `lenderId → LenderProfile` pasa a apuntar a `lenderCompanyId → LenderCompany` | Requisito explícito y no ambiguo del encargo. Un `LenderProfile` sigue existiendo como identidad de la persona, pero ya no puede ser la unidad de aislamiento si esa misma persona opera varias empresas | Afecta `Contract`, `AuditLog`, `Property` (nueva) — y, fuera del alcance directo de esta fase pero **marcado como impacto pendiente**, el diseño de `withTenantScope` (Fase 3) y todos los endpoints `/api/lenders/me/*` (Fase 4/5/6), que hoy asumen un único `lenderId` por sesión y ahora necesitan resolver **qué empresa** está activa en cada request — ver sección [15](15-riesgos-y-decisiones-pendientes.md) |
| `D-P1-4` | `BorrowerProfile.lenderId` `NOT NULL` — un deudor pertenece a un único prestamista ([04](04-base-de-datos.md) v1) | "Un usuario BORROWER podrá tener múltiples préstamos, préstamos con diferentes lenders" | [04-base-de-datos.md](04-base-de-datos.md) v1 — **y** ya en conflicto interno con [IMPLEMENTATION_PROGRESS.md](../IMPLEMENTATION_PROGRESS.md) (decisión `C-1`/`M-1`, fechada 3 sep, nunca integrada aquí) | Se elimina `BorrowerProfile.lenderId`. Se agrega tabla puente `LenderBorrower` (N:M entre `LenderCompany` y `BorrowerProfile`) | Formaliza una decisión que el equipo **ya había tomado** el 3 sep (post Product Board) pero que quedó viviendo solo en el checklist de progreso, nunca aplicada al plan base ni a los tickets de Fase 1 — este documento cierra esa brecha | `BorrowerProfile`, nueva tabla `LenderBorrower`, regla de servicio de `ContractBorrower` (ya no compara `lenderId` directo) |
| `D-P1-5` | Dirección embebida directamente en `Contract`, sin tabla `Property` — decisión `A-4`, justificada explícitamente por "instrucción del encargo" original | "Se necesita una nueva tabla para almacenar la información de la propiedad" con campos de ARV/valuation/taxes | `A-4` (esta misma sección, arriba) y [05-que-no-copiar-de-owner.md](05-que-no-copiar-de-owner.md) ("no existe tabla `Property` en absoluto") | Se crea `Property` como tabla propia. `Contract.propertyId` FK obligatoria simple (no tabla puente): 1 `Property` puede tener N `Contract` en el tiempo, 1 `Contract` tiene exactamente 1 `Property` | Instrucción explícita y más reciente reemplaza a `A-4`. Ver justificación completa de cardinalidad en [04-base-de-datos.md §4.3](04-base-de-datos.md#property-nueva) | `Contract` pierde 8 campos de dirección/tipo, gana `propertyId`; nueva tabla `Property`; orden de migración cambia (Property antes que Contract) |
| `D-P1-6` | No existía | Bookkeeper: acceso de auditoría/registros financieros, sin autoridad de decisión sobre préstamos | — (no había contradicción, es una entidad nueva) | `BookkeeperProfile` (1:1 `User`) + tabla puente `LenderCompanyBookkeeper` (N:M con `LenderCompany`), mismo patrón que `LenderBorrower` | Un despacho contable puede llevar los libros de varias empresas prestamistas pequeñas — mismo razonamiento N:M que ya se aplicó a Borrower. La restricción de "no decide préstamos" es de autorización (Fase 3), no de datos |
| `D-P1-7` | No existía | Insurance Company puede asociarse al contrato al momento de la firma; Product Board describe integración de seguros más amplia (EOI, bidding de carriers) como "Coming Soon" | — | `InsuranceCompanyProfile` (1:1 `User`, directorio de plataforma, **no** tenant-scoped) + `Contract.insuranceCompanyId` (nulo hasta la firma). EOI/bidding/mortgagee-clause-autofill **no se modelan** en esta fase | El Product Board marca la integración completa de seguros como "Coming Soon" — solo se construye la estructura mínima que el encargo pide ahora (asociación al contrato) |
| `D-P1-8` | `LenderProfile.status` (`LenderStatus` `ACTIVE`/`SUSPENDED`) controlaba login a nivel de persona | Con `D-P1-2` (activación de usuario) y `D-P1-3` (empresa ≠ persona), ese campo queda ambiguo: ¿suspende a la persona o a la empresa? | [04-base-de-datos.md](04-base-de-datos.md) v1 | `LenderStatus` se elimina de `LenderProfile`. Se agrega `LenderCompanyStatus` en `LenderCompany` — suspensión por empresa, no por persona (el login por persona ya lo cubre `User.isActive`) | Evita que un campo represente dos cosas distintas a la vez ahora que existen dos niveles (persona vs. empresa) |
| `D-P1-9` | `BorrowerProfile.notes` — notas internas del prestamista sobre el deudor, en la tabla del deudor | Con `D-P1-4` (deudor multi-prestamista), esas notas dejarían de ser privadas entre prestamistas distintos | [04-base-de-datos.md](04-base-de-datos.md) v1 (implícito: nunca se previó que `BorrowerProfile` fuera compartido) | `notes` se mueve de `BorrowerProfile` a `LenderBorrower` | Evita una fuga de información entre tenants que el modelo anterior no podía prevenir una vez que `D-P1-4` se aplica |
| `D-P1-10` | `LenderProfile.createdByAdminId` obligatorio (`NOT NULL`) — todo Prestamista nace dado de alta por un Admin (`BE-040`); `BorrowerProfile.createdByUserId` ya era nulo pero solo se justificaba como "posible alta futura" (`PB-013`) | Spencer aclaró (mensaje posterior a la primera pasada de esta ronda): "los usuarios con los roles lender y borrower tienen la capacidad de crearse ellos mismos" — se confirmó explícitamente que esto es **adicional** a los flujos existentes (Admin crea Lender, Lender crea/vincula Borrower), no un reemplazo — ver pregunta de desambiguación planteada y respuesta elegida ("Se suma") | `LenderProfile.createdByAdminId` en [04-base-de-datos.md](04-base-de-datos.md) (era `Uuid → User` obligatorio) | `LenderProfile.createdByAdminId` pasa a `Uuid? → User` opcional (mismo patrón que `BorrowerProfile.createdByUserId`). `ADMIN`, `BOOKKEEPER` e `INSURANCE_COMPANY` siguen sin poder auto-registrarse — el auto-registro es exclusivo de `LENDER`/`BORROWER` | Un Prestamista auto-registrado no tiene un Admin actor en el momento de creación; forzar el campo a `NOT NULL` haría imposible representar ese caso. Migración aplicada: `20260904234340_lender_profile_self_registration` |

**Consecuencia sobre `IMPLEMENTATION_PROGRESS.md`**: las decisiones `C-1`/`M-1`/`M-4`/`M-5`/`M-6` de ese documento quedan **formalizadas e integradas** por `D-P1-3`, `D-P1-4` y las notas de la sección [4.3](04-base-de-datos.md#43-tablas) sobre `Transaction`/`Autopay` — dejan de ser deltas sueltos y pasan a ser parte del plan base. Ver el backlog reescrito en [16. Fase 1 — plan actualizado](16-fase-1-actualizada.md).

---

## Decisiones 2026-09-06 (ronda Fase 2)

Antes de implementar la Fase 2 (Authentication), Spencer resolvió explícitamente tres puntos que el plan había dejado contradictorios o abiertos — ver [17. Fase 2 — plan actualizado](17-fase-2-actualizada.md) para el detalle completo de implementación.

| # | Comportamiento actual | Requisito nuevo | Conflicto con | Decisión | Motivo |
|---|---|---|---|---|---|
| `D-P2-1` | `IMPLEMENTATION_PROGRESS.md` (antes de esta ronda) listaba `PB-013` como "alta propia del deudor, rol BORROWER" — contradiciendo a `D-P1-10` (arriba), que ya decía ambos roles. Además, el riesgo #20 de [15](15-riesgos-y-decisiones-pendientes.md) dejaba abierto si un auto-registrado queda operativo de inmediato o necesita aprobación, y el riesgo #21 dejaba abierto si el registro pide contraseña/empresa en el mismo paso | Spencer, textual: "los usuarios lender y borrower si pueden autoregistrarse... de la parte de la Activación ayudame a generar ese endpoint... del post registro... una vez activada la cuenta se le enviara un correo con la contraseña generada" | `IMPLEMENTATION_PROGRESS.md` (`PB-013`, antes de esta ronda) vs. `D-P1-10`; riesgos #20 y #21 de [15](15-riesgos-y-decisiones-pendientes.md) | (a) `LENDER` y `BORROWER` pueden auto-registrarse vía `POST /api/auth/register`, sin contraseña — solo `{ name, email, role }`. (b) La cuenta nace `isActive=false`. (c) Nuevo endpoint `POST /api/admin/users/:id/activate` (ticket `BE-097`, ADMIN-only): en la primera activación genera una contraseña temporal y la envía por correo (plantilla `account-activated`); una reactivación posterior no la toca. `POST /api/admin/users/:id/deactivate` es su contraparte, y además revoca los refresh tokens vigentes | Cierra la contradicción con una única fuente de verdad (este documento + [17](17-fase-2-actualizada.md)), resuelve el riesgo #20 (sí necesita aprobación de un Admin) y el #21 (el registro no pide ni contraseña ni empresa — la empresa se da de alta después, Fase 4) |
| `D-P2-2` | §7.1 de [07-autenticacion-y-autorizacion.md](07-autenticacion-y-autorizacion.md) especificaba el payload del access token como `{ sub, role, lenderId? }`, resuelto en el login | Ya no es implementable tal cual: desde `D-P1-3` el tenant es `LenderCompany`, y un `LenderProfile` puede tener N — no hay un único `lenderId` que resolver en el login | §7.1 de [07](07-autenticacion-y-autorizacion.md) (ya señalado como impacto pendiente por `D-P1-3` y el riesgo #18 de [15](15-riesgos-y-decisiones-pendientes.md)) | El payload implementado es `{ sub, role }`, sin `lenderId`. `GET /api/auth/me` devuelve la lista completa de `LenderCompany`/vínculos `LenderBorrower` del usuario en su lugar | El riesgo #18 ya marcaba este rediseño como necesario antes de Fase 3; esta ronda lo resuelve para lo que le corresponde a Fase 2 (qué lleva el JWT) y deja explícitamente para Fase 3 (`withTenantScope`, `BE-037`) cómo el cliente indica con qué empresa está operando en cada request |
| `D-P2-3` | Sin precedente explícito — `RefreshToken.tokenHash`/`PasswordResetToken.tokenHash`/`TwoFactorRecoveryCode.codeHash` ya existían como `@unique` desde `BE-021` (Fase 1), sin que ningún documento fijara con qué algoritmo se hashean | Implementar `BE-025`/`BE-032`/`BE-034` exige elegir un algoritmo de hash para esas tres columnas | — (decisión de implementación, no había contradicción previa) | `User.password` sigue con bcrypt (`env.bcryptCost`). Los tres campos opacos de arriba usan **SHA-256** en su lugar | bcrypt saltea distinto en cada llamada — no sirve para buscar por índice en una columna `@unique`, obligaría a comparar contra todas las filas. SHA-256 es determinista y estos valores ya tienen 256 bits de entropía propia (`crypto.randomBytes(32)`), así que un hash rápido no los debilita |
| `D-P2-4` | El CRUD de usuarios de Fase 0 (`GET`/`POST`/`PATCH`/`DELETE /api/users`) no verificaba sesión ni rol — cualquiera podía listar/crear/editar/borrar usuarios (hueco documentado explícitamente en `Docs/API_REFERENCE.md`). Tampoco había forma de fijar `isActive` fuera de `/api/admin/users/:id/activate|deactivate`, ni un campo de teléfono en `User`, ni un endpoint para que un usuario edite su propia información | Spencer, 2026-09-07, textual: "que solo el admin pueda hacer uso de esas apis... se mande el valor de isActive para que el admin pueda ya activar el usuario o dejarlo desactivado... como ahorita no se tiene lo del correo por lo mientras genera un contraseña y muestrala en la respuesta... se agregue el campo phone... falto una api más, que el usuario pueda editar su información personal el mismo" | `Docs/API_REFERENCE.md` (aviso "sin protección de sesión todavía") | (a) Los 4 endpoints de `/api/users` exigen sesión ADMIN. (b) `createUserSchema`/`updateUserSchema` aceptan `isActive`; en el `PATCH`, una transición `false→true` corre la misma lógica que `BE-097` (contraseña temporal solo en la primera activación) — la lógica vive una única vez en `src/services/userActivation.service.ts`, reusada por `adminUsers.service.ts` y `users.service.ts` para no duplicarla. (c) `User.phone String?` nuevo (`@db.VarChar(10)`, migración `20260907213645_add_user_phone`), validado como 10 dígitos exactos en Zod; usado en `POST`/`PATCH /api/users` y en `POST /api/auth/register`. (d) Nuevo `PATCH /api/auth/me` (`BE-099`) — autoservicio, solo `name`/`phone`, nunca `email`/`password`/`role`/`isActive`. (e) Mientras no haya proveedor de correo real, la contraseña generada al activar (primera vez) se devuelve también en el JSON de la respuesta (`temporaryPassword`), no solo por correo | Cierra un hueco de seguridad real (CRUD sin ninguna protección) y una limitación operativa concreta (sin correo real, el Admin no tenía cómo enterarse de la contraseña generada) — ver tickets `BE-098`/`BE-099` en [13. Backlog detallado](13-backlog.md) |
| `D-P2-5` | `D-P2-4` (arriba) todavía dejaba `POST /api/users` pidiendo `password` como campo obligatorio del body — inconsistente con el resto del sistema, donde toda cuenta nueva (auto-registro, `D-P2-1`) nace sin contraseña propia y la recibe recién al activarse. `generateTemporaryPassword()` generaba además una cadena de 16 caracteres con símbolos, pensada para copiar/pegar, no para dictar | Spencer, 2026-09-07, textual: "el password no se pide en la fase o en la creación de usuarios, mas bien el usuario se pone true en isActive imprimes una contrasena generica de 8 digitos en la respuesta" | `createUserSchema` (pedía `password` obligatorio); `generateTemporaryPassword()` (16 caracteres alfanuméricos + símbolos) | (a) `createUserSchema` ya **no** tiene `password` — `POST /api/users` nunca lo acepta. La cuenta nace con un valor aleatorio inutilizable (mismo patrón que el auto-registro) y, si el resultado es una cuenta activa (`isActive:true` explícito, o sin el campo — sigue siendo el default), `createUser()` reusa `activateUserAccount()` (la misma función de `D-P2-4`/`BE-097`) para generar la contraseña real y devolverla en la respuesta (`temporaryPassword`), exactamente igual que activar por `PATCH` o por el endpoint dedicado. (b) `generateTemporaryPassword()` pasa de 16 caracteres alfanuméricos+símbolos a **8 dígitos numéricos** — cambio que aplica parejo a los tres caminos que la usan (`POST /api/users`, `PATCH /api/users/:id`, `POST /api/admin/users/:id/activate`), porque los tres llaman a la misma función | Una única función de generación (ya existente desde `BE-097`) evita que cada camino de activación tenga su propio formato de contraseña; 8 dígitos numéricos es más fácil de comunicar verbalmente o por escrito que una cadena con símbolos — a costa de menos entropía (10⁸ combinaciones), aceptable para una credencial temporal de un solo uso que además ya viaja hasheada con bcrypt |

---

## Decisiones 2026-09-08 (ronda Fase 3)

Antes de implementar la Fase 3 (Authorization), dos puntos del plan
original quedaban ambiguos o dependían de endpoints que todavía no existen.
Se resolvieron con el usuario antes de tocar código — ver
[Fase 3 — Authorization](fases/fase-03-authorization.md) para el detalle
ticket por ticket.

| # | Comportamiento actual | Requisito nuevo | Conflicto con | Decisión | Motivo |
|---|---|---|---|---|---|
| `D-P3-1` | §7.4 dice que un `ADMIN`/`LENDER` sin 2FA activo "no puede hacer ninguna operación de escritura fuera de `/api/auth/2fa/*`" — leído literal, eso bloquearía también `PATCH /api/auth/me` (editar el propio nombre/teléfono) o `POST /api/auth/logout` | Implementar `withRole` (`BE-036`) exige decidir el alcance exacto de esa regla — el texto de §7.4 y la tabla de §7.6 no son 100% consistentes entre sí sobre si aplica a *todo* o solo a acciones administrativas | §7.4 vs. §7.6 de [07](07-autenticacion-y-autorizacion.md) (ambigüedad interna) | La regla aplica **solo a endpoints de negocio ADMIN/LENDER-only** que ya pasan por `withRole(["ADMIN"\|"LENDER"], ...)` para una acción administrativa (hoy: `POST`/`PATCH`/`DELETE /api/users`, `POST /api/admin/users/:id/activate\|deactivate`). Autoservicio de cualquier rol (`logout`, `logout-all`, `GET`/`PATCH /api/auth/me`) y las propias rutas `/api/auth/2fa/*` quedan exentos — con `requireTwoFactor:false` explícito en estas últimas. Dentro de los endpoints de negocio, la regla es solo sobre **escrituras**: `GET /api/users` queda exento | Sin esta acotación, un Lender recién logueado no podría ni cerrar sesión ni corregir su propio teléfono antes de activar 2FA — un candado más estricto que lo que el negocio pidió. La versión acotada sigue cumpliendo el objetivo real de §7.4: nadie hace una acción administrativa sin haber asegurado su cuenta primero |
| `D-P3-2` | `withTenantScope` (`BE-037`) está diseñado para endpoints que no existen todavía (`/api/lenders/me/*`, `/api/contracts/*` — Fase 4/5/6). El riesgo #18 ya señalaba que hacía falta decidir cómo el cliente indica "con qué `LenderCompany` estoy operando", pero sin un endpoint real no hay nada contra qué validar esa decisión | Implementar Fase 3 requería decidir si construir `withTenantScope` ahora (especulativo) o después | Riesgo #18 de [15](15-riesgos-y-decisiones-pendientes.md) | `withTenantScope` se difiere a Fase 4 — se decide su mecanismo (header, path, etc.) cuando exista el primer endpoint tenant-scoped real. Esta fase sí entrega lo que **no** dependía de eso: `withAuth`/`withRole` (con la regla de `D-P3-1`) y `requireContractAccess` (`BE-038`, resuelto sin necesitar `withTenantScope` — compara `contract.lenderCompanyId` contra **todas** las `LenderCompany` del `LenderProfile`, sin depender de cuál esté "activa") | Construir un mecanismo de selección de tenant sin un consumidor real (ni de negocio ni de frontend) sería adivinar una decisión de producto que le corresponde a Fase 4. El riesgo #18 pasa de "bloquea Fase 3" a "bloquea específicamente Fase 4" |

---

## Decisiones 2026-09-08 (ronda Fase 4/5)

Al llegar el momento de construir los primeros endpoints tenant-scoped
(`/api/lenders/me/*`), el riesgo #18/`D-P3-2` (arriba) finalmente tenía un
caso real contra el cual resolverse. También aparecieron dos huecos más:
BE-050 pedía bloquear a un Borrower hasta que cambie su contraseña temporal
sin que existiera ninguna columna para distinguir ese estado, y el mapa de
endpoints (§6.3) tenía `GET`/`PATCH /api/lenders/me` sin ningún ticket
detrás. Ver [Fase 4 — Admin/Lenders](fases/fase-04-admin-lenders.md) y
[Fase 5 — Borrowers](fases/fase-05-borrowers.md) para el detalle ticket por
ticket.

| # | Comportamiento actual | Requisito nuevo | Conflicto con | Decisión | Motivo |
|---|---|---|---|---|---|
| `D-P4-1` | `withTenantScope` seguía diferido (`D-P3-2`) — Fase 4/5 ya necesitan resolver acceso multi-empresa de verdad (`GET/POST/PATCH/DELETE /api/lenders/me/borrowers[/:id]`) | Implementar BE-045..049 exige decidir cómo un Lender con N `LenderCompany` opera sobre sus deudores, sin un selector de "empresa activa" construido | Riesgo #18 de [15](15-riesgos-y-decisiones-pendientes.md); `BE-045` original decía `BorrowerProfile.lenderId` (ya no existe, ver `D-P1-4`) | **Lecturas** (`GET .../borrowers`, `GET .../borrowers/:id`): resuelven contra **todas** las `LenderCompany` del Lender — mismo patrón que `requireContractAccess` (`BE-038`). **Creación** (`POST .../borrowers`): con una sola empresa se resuelve sola; con más de una, el body debe traer `lenderCompanyId` explícito, validado contra las empresas del Lender (`404` si no es una de las suyas — anti-enumeración, §7.5). **Edición/baja de una relación existente** (`PATCH`/`DELETE .../borrowers/:id`): la relación (`LenderBorrower`) ya fija la empresa, no se pide nada nuevo | Resuelve lo que Fase 4/5 necesitaban sin inventar un mecanismo de selección de tenant sin frontend que lo consuma. `withTenantScope` como middleware genérico sigue sin construirse — queda para cuando Fase 6 (Contratos) tenga un flujo donde "operar como la Empresa A" persista a lo largo de varias acciones seguidas |
| `D-P4-2` | `BE-050` pide bloquear al Borrower (`PATCH /api/borrowers/me`) hasta que cambie la contraseña temporal generada al crearlo — pero no hay ninguna columna que distinga "sigue siendo la generada" de "el usuario ya la cambió". `lastLoginAt` no sirve: el login que trae la sesión actual ya lo dejó en no-nulo | Implementar `BE-050` exige un signal real para `mustChangePassword` | Ninguno — hueco de diseño, no contradicción entre documentos | Se agrega `User.mustChangePassword Boolean @default(false)` (migración aditiva). Se pone `true` cada vez que se genera una contraseña temporal (`issueTemporaryPassword()`, usado por `activateUserAccount`/`BE-097`, `POST /api/admin/lenders`/`BE-040`, `POST /api/lenders/me/borrowers`/`BE-045`). Se pone `false` al cambiarla intencionalmente (`password/reset`/`BE-032`, el nuevo `POST /api/borrowers/me/password`/`BE-050`, o un `password` explícito puesto a mano por un Admin vía `PATCH /api/users/:id`). Se expone en `SafeUser`. `withRole` gana la opción `requirePasswordChanged`, aplicada solo a `PATCH /api/borrowers/me` (alcance literal del ticket) | Reusa la misma consulta en vivo que ya hace `withRole` para el chequeo de 2FA (`D-P3-1`) en vez de agregar un chequeo separado. No se retrofittea a endpoints ya construidos en fases anteriores |
| `D-P4-3` (`BE-100`, nuevo) | El mapa de endpoints (§6.3 de [06](06-api-endpoints.md)) lista `GET`/`PATCH /api/lenders/me` desde el plan original, pero ningún ticket de Fase 4 ni Fase 5 lo cubre — es el equivalente de `BE-050` para el lado Lender | Implementar el mapa de endpoints completo exige que ese par tenga un ticket real detrás | §6.3 de [06](06-api-endpoints.md) sin ticket asignado | Se agrega `BE-100`: `GET`/`PATCH /api/lenders/me`, mismos campos editables que `PATCH /api/admin/lenders/:id` (`BE-043` — solo `LenderProfile.contactPhone`; `name`/`phone` de `User` siguen en `PATCH /api/auth/me`, `BE-099`) | Mismo criterio ya usado con `BE-097`/`098`/`099`: cerrar un hueco del backlog documentándolo como ticket nuevo, no dejarlo como una nota suelta en el mapa de endpoints |

---

## Decisión 2026-09-08 (rescopeo post-Fase 4, `D-P4-5`)

Con Fase 4/5 ya implementadas y en pruebas manuales, Spencer señaló un problema real de diseño en `BE-040` (`POST /api/admin/lenders`): ese endpoint creaba `User`+`LenderProfile`+`LenderCompany` en un solo paso, duplicando (con otro shape de body) lo que el auto-registro (`POST /api/auth/register`, `D-P2-1`) ya resuelve para la persona — y, peor, dejaba a todo Lender auto-registrado **permanentemente sin ninguna forma de crearse una empresa**, porque ningún endpoint existente cubría "agregar una `LenderCompany` a un `LenderProfile` que ya existe".

| # | Comportamiento actual | Requisito nuevo | Conflicto con | Decisión | Motivo |
|---|---|---|---|---|---|
| `D-P4-5` | `POST /api/admin/lenders` (`BE-040`) pedía `name`+`email` (persona) junto con `companyName`+`ein`+dirección (empresa) y creaba las tres tablas (`User`+`LenderProfile`+`LenderCompany`) de una — el único camino para que un Lender tuviera una `LenderCompany` | Spencer, textual: "mi idea no es que se vuelva a registrar el lender, si no que el usuario con rol lender tenga la oportunidad de crear su compañia o compañias... esta api ya no necesitaria mandar el nombre del usuario ni el correo, que solo se asocie con el id... que esta api se pueda usar ya si eres un lender creando compañias asociadas a el o si el admin le quiere asociar una compañia a un usuario tipo lender" | `POST /api/admin/lenders` (`BE-040`); dejaba sin resolver el caso de un Lender auto-registrado (`D-P2-1`) sin ninguna `LenderCompany` | Se separa "dar de alta la persona" de "darle una empresa". La persona ya nace por auto-registro (sin tocar) o por cualquier otro alta de `User` que exista. `POST /api/admin/lenders` (el `POST` de nivel raíz) **se elimina** — `GET`/`PATCH`/`DELETE /api/admin/lenders/:id` y `GET /api/admin/lenders` (`BE-041..044`) siguen igual, operan sobre un `LenderProfile` que ya existe. Dos rutas nuevas, mismo servicio compartido (`lenders.service.ts#createLenderCompany`), un schema sin `name`/`email` (`createLenderCompanySchema`): **`POST /api/admin/lenders/:id/companies`** (ADMIN, `:id`=`LenderProfile.id`, asocia una empresa a un Lender existente) y **`POST /api/lenders/me/companies`** (`BE-101`, nuevo — LENDER, autoservicio, resuelve el `LenderProfile` desde la sesión). El tenant real sigue siendo `LenderCompany` — ninguna de las dos rutas toca `User`, y sigue siendo la empresa (no la persona) la que aparece en `Contract.lenderCompanyId` | Cierra el hueco real (un Lender auto-registrado no podía operar nunca) sin reintroducir la alta de persona por dos caminos distintos (auto-registro vs. admin) que podían divergir. Reusa exactamente el patrón ya validado de `D-P4-1` (una misma función de service, dos rutas — una admin, una self-service) |

**Impacto en tests**: `src/services/lenders.service.integration.test.ts` y `src/app/api/admin/lenders/lenders-routes.integration.test.ts` se reescribieron contra `createLenderCompany`/`createOwnLenderCompany` en vez de `createLender` (eliminada); nuevo `src/app/api/lenders/me/companies/lender-companies-routes.integration.test.ts`. Nueva fixture `createTestLenderWithoutCompany()` en `src/db/testFixtures.ts` para el caso "Lender activo, cero empresas". Sin migración — `LenderCompany` ya tenía todas las columnas necesarias.

---

## Decisión 2026-09-10 (ronda Fase 6 — arranque, `D-P6-1`: `withTenantScope` descartado)

Antes de escribir el primer ticket real de Fase 6, el riesgo #18 (ver [15](15-riesgos-y-decisiones-pendientes.md)) seguía señalando a `withTenantScope` (`BE-037`) como "lo primero que hay que decidir" al llegar acá — la propia nota de Fase 3 en [IMPLEMENTATION_PROGRESS.md](../IMPLEMENTATION_PROGRESS.md) ya lo anticipaba: *"diferido de verdad a Fase 6, el primer módulo con un flujo donde 'operar como la Empresa A' persiste a lo largo de varias acciones seguidas"*. Al llegar el momento de decidirlo de verdad, la pregunta ya no era abstracta: `BE-051` (`POST /api/contracts`) lo listaba como dependencia dura.

| # | Comportamiento actual | Requisito nuevo | Conflicto con | Decisión | Motivo |
|---|---|---|---|---|---|
| `D-P6-1` | `BE-051` depende de `BE-037` (`withTenantScope`), nunca construido — diferido de Fase 3 → Fase 4 (`D-P3-2`) → Fase 6 (nota de Fase 3 en [IMPLEMENTATION_PROGRESS.md](../IMPLEMENTATION_PROGRESS.md)), sin que ningún módulo intermedio (Fase 4, 5, ni la Fase 13 recién diseñada) haya terminado necesitándolo | Fase 6 necesita arrancar sin quedar bloqueada esperando un middleware que ya se evitó dos veces | Riesgo #18 de [15](15-riesgos-y-decisiones-pendientes.md); dependencia `BE-037` en `BE-051` ([fases/fase-06-contracts.md](fases/fase-06-contracts.md)) | **Se descarta construir `withTenantScope` como middleware genérico — definitivamente, no "diferido otra vez".** `BE-051` sigue el mismo patrón ya usado dos veces (`D-P4-1`, Fase 5; `D-S2-20`, Fase 13): `lenderCompanyId` opcional en el body si el Lender que crea el contrato tiene una sola `LenderCompany` (se resuelve sola), obligatorio si tiene más de una (`400 LENDER_COMPANY_REQUIRED`, mismo código de error ya implementado en Fase 5), `404` si el `lenderCompanyId` enviado no es una de las suyas. La dependencia de `BE-051` pasa de `BE-037` a `BE-012` (`ContractTerms`). `BE-037` queda formalmente **descartado** como ticket — no reaparece en ninguna fase futura salvo que un caso de uso real y concreto lo justifique | Tres módulos distintos (Borrowers/Fase 5, Marketplace/Fase 13, y ahora Contracts/Fase 6) necesitaron resolver "con qué empresa estoy operando" y los tres se resolvieron con la misma regla simple, sin middleware dedicado — construirlo ahora sería una abstracción para un problema que ya se resolvió tres veces de forma más barata. El caso original que motivó `D-P3-2` (una empresa activa que "se pega" a la sesión durante varias acciones seguidas) nunca apareció en la práctica; si aparece en el futuro, se reconsidera con un caso real contra qué diseñarlo, no especulativamente |

**Impacto documentado en**: [15 (riesgo #18, marcado resuelto)](15-riesgos-y-decisiones-pendientes.md), [07-autenticacion-y-autorizacion.md §7.1/7.5/7.6](07-autenticacion-y-autorizacion.md), [03-arquitectura-backend.md §3.1](03-arquitectura-backend.md), [fases/fase-03-authorization.md](fases/fase-03-authorization.md) (`BE-037`), [fases/fase-06-contracts.md](fases/fase-06-contracts.md) (`BE-051`), [fases/fase-07-payments.md](fases/fase-07-payments.md) (`BE-072`), [fases/fase-08-testing.md](fases/fase-08-testing.md) (`BE-078`), [fases/README.md](fases/README.md) (índice de tickets), [13-backlog.md](13-backlog.md), [Docs/API_REFERENCE.md](../API_REFERENCE.md), [Docs/IMPLEMENTATION_PROGRESS.md](../IMPLEMENTATION_PROGRESS.md), `src/middlewares/README.md`, `src/auth/jwt.ts` (comentario). Las filas históricas `D-P1-3`, `D-P2-2`, `D-P3-2`, `D-P4-1` (arriba) **no se editan** — describen correctamente el estado de la decisión en su momento; esta es la que la cierra.

---

## Decisión 2026-09-08 (idioma de las respuestas de la API, `D-P4-6`)

| # | Comportamiento actual | Requisito nuevo | Conflicto con | Decisión | Motivo |
|---|---|---|---|---|---|
| `D-P4-6` | Los mensajes de `AppError`/Zod/`apiError` estaban mezclados: la mayoría en español, pero algunos ya en inglés (`withRole.ts`, `REGISTER_RESPONSE_MESSAGE`, el fallback 500 de `errorHandler.ts`) — inconsistencia que se había señalado como posible error sin corregirla todavía | Spencer, textual: "las respuestas de las apis siempre deben estar en ingles" | Toda la Fase 1-5 tenía mensajes de error/validación en español | **Todo el `message` que viaja en una respuesta HTTP de la API va en inglés** — de ahora en más y con efecto retroactivo sobre lo ya construido: los ~51 `new AppError(...)` de `src/middlewares/`, `src/services/`, `src/auth/jwt.ts`; los mensajes de validación Zod de `src/validations/*.ts`; el `apiError("JSON inválido en el body", ...)` repetido en cada `route.ts` (ahora `"Invalid JSON in request body"`). El resto del proyecto (comentarios de código, commits, esta documentación) **sigue en español** — la convención es específica de lo que un cliente de la API recibe, no del idioma de trabajo del equipo | Consistencia para quien consuma la API (o la muestre tal cual en un frontend) — un mismo cliente no debería recibir mensajes en dos idiomas según qué endpoint falle |

**Cobertura verificada**: sin `.test.ts` que compare `.message` contra un literal en español (los tests comparan `code`, o `.message` entre dos respuestas para igualdad relativa — anti-enumeración de login, §7.3) — la traducción no rompió ningún assert. Emails (`src/lib/email.ts`, plantilla `account-activated`) quedaron **fuera** de este cambio a propósito: un correo no es una respuesta de la API; si también deben pasarse a inglés, es una decisión aparte todavía no pedida.

---

## Decisión 2026-09-08 (`:id` de `/api/admin/lenders*` acepta también `User.id`, `D-P4-7`)

| # | Comportamiento actual | Requisito nuevo | Conflicto con | Decisión | Motivo |
|---|---|---|---|---|---|
| `D-P4-7` | `GET/PATCH/DELETE /api/admin/lenders/:id` y `POST /api/admin/lenders/:id/companies` resolvían `:id` únicamente contra `LenderProfile.id` (`requireLenderProfile`, `lenders.service.ts`) | Spencer reportó `404 LENDER_NOT_FOUND` al usar el `id` que devuelve `GET /api/users` — que es el único endpoint donde un Admin ve el `id` de un Lender sin pasar por `GET /api/admin/lenders`, y ese `id` es `User.id`, no `LenderProfile.id` (dos ids distintos para lo que un Admin percibe como "un solo Lender") | `lenders.service.ts#requireLenderProfile` | `requireLenderProfile` ahora resuelve por `LenderProfile.id` **o** `User.id` (`prisma.lenderProfile.findFirst({ where: { OR: [{id}, {userId: id}] } })`). Cubre de una los 4 endpoints que la usan (`GET`/`PATCH`/`DELETE /api/admin/lenders/:id`, `POST .../companies`) | El camino "natural" para que un Admin encuentre el id de un Lender (`GET /api/users`) nunca iba a devolver `LenderProfile.id` — exigírselo era forzar a pasar primero por `GET /api/admin/lenders` y cruzar por email/nombre, un paso que ningún ticket pedía explícitamente. Aceptar ambos ids es más forgiving sin ambigüedad real (namespaces de UUID distintos, colisión negligible) |

---

## Decisión 2026-09-08 (`LenderProfile.contactPhone` eliminado + PATCH/DELETE de `LenderCompany` puntual, `D-P4-8`)

| # | Comportamiento actual | Requisito nuevo | Conflicto con | Decisión | Motivo |
|---|---|---|---|---|---|
| `D-P4-8` | `LenderProfile.contactPhone` era el único campo propio editable de `LenderProfile` (`PATCH /api/admin/lenders/:id` / `PATCH /api/lenders/me`, `BE-043`/`BE-100`). No existía ningún endpoint para editar o borrar una `LenderCompany` puntual (`companyName`/`ein`/dirección/`isOpenToDeals`/`status`) | Spencer, textual (dos mensajes): sobre `contactPhone` — cuestionó para qué servía, dado que las empresas son del usuario Lender; se confirmó que es redundante con `User.phone` (agregado después, `D-P2-4`, como teléfono de cuenta genérico para todos los roles — nadie reconcilió `LenderProfile.contactPhone` contra eso cuando se agregó). Luego, textual: "sacá contactPhone y hacé la migración además necesitaria las apis para editar los datos de la compañia" | `LenderProfile.contactPhone` (schema); huecos ya anotados en `04-base-de-datos.md`/`API_REFERENCE.md` sobre falta de edición de `LenderCompany` puntual | (a) `LenderProfile.contactPhone` se elimina (migración `20260908234923_remove_lender_profile_contact_phone`) — `LenderProfile` queda sin ningún campo propio editable, así que `PATCH /api/admin/lenders/:id` y `PATCH /api/lenders/me` **se eliminan** (nada que editar). (b) Nuevas rutas **`PATCH`/`DELETE /api/admin/lenders/:id/companies/:companyId`** (Admin únicamente — no se pidió autoservicio del Lender sobre sus propias empresas): `PATCH` edita cualquier campo de la empresa (incluidos `status`/`isOpenToDeals`, con `EIN_TAKEN` si el nuevo `ein` ya lo usa otra); `DELETE` hace soft-delete de **esa sola empresa** (no de todo el Lender, a diferencia de `DELETE /api/admin/lenders/:id`/`BE-044`), bloqueado por `LENDER_HAS_ACTIVE_CONTRACTS` si tiene un `Contract` `ACTIVE`/`DELINQUENT`. Nuevo código `404 LENDER_COMPANY_NOT_FOUND` si `:companyId` no es una empresa de `:id` | `LenderProfile` seguía teniendo sentido como tabla (ancla de `LenderCompany`, `createdByAdminId`, `notes` — mismo patrón que `BorrowerProfile`/`BookkeeperProfile`/`InsuranceCompanyProfile`), pero `contactPhone` específicamente no tenía ninguna razón de negocio para existir aparte de `User.phone`. Las rutas de empresa puntual cierran un hueco operativo real: sin ellas, un Admin no podía corregir un typo en `companyName`/`ein`/dirección, ni suspender una empresa (`D-P1-8`) ni marcarla "Not Taking Loans" (`isOpenToDeals`, `M-4`) después de creada |

**Impacto en tests**: se reescribieron `src/services/lenders.service.integration.test.ts` (quita los tests de `updateLender`/`updateOwnLenderProfile`, agrega `updateLenderCompany`/`deleteLenderCompany`) y `src/app/api/admin/lenders/lenders-routes.integration.test.ts` (el camino dorado ahora edita/borra la empresa en vez del `LenderProfile`). `GET /api/auth/me` y el shape de `Lender` en `API_REFERENCE.md` ya no incluyen `contactPhone` de `LenderProfile` (el de `LenderCompany` sigue existiendo, sin cambios).

---

## Decisiones 2026-09-10 (ronda Product Spec v2 + Commitment Letter Spec)

Spencer entregó dos documentos nuevos — [pml_product_spec_v2.pdf](../pml_product_spec_v2.pdf) (v2, 9 sep 2026) y [pml_commitment_letter_spec 1.1.pdf](../pml_commitment_letter_spec%201.1.pdf) (v1.1, 9 sep 2026) — que describen PayMyLoan.ai como **marketplace** (no solo un backend de servicing) con flujo de originación, estructura de fees, vetting del deudor, dashboards, notificaciones y ratings que **no estaban modelados** en ninguna versión previa de este plan más allá de menciones sueltas en [IMPLEMENTATION_PROGRESS.md](../IMPLEMENTATION_PROGRESS.md) (`PB-009`..`PB-013`, sin detalle). Fases 0–5 (`BE-001`..`BE-102`) ya están implementadas y **no se tocan** — todo lo de abajo afecta a partir de Fase 6 en adelante. Mismo método que las rondas anteriores: contradicción/hueco → decisión → impacto concreto. Prefijo `D-S2-*` (Spec v2) para no colisionar con futuros `D-P6-*` de la propia implementación de Fase 6.

| # | Comportamiento actual | Requisito nuevo | Conflicto con | Decisión | Motivo |
|---|---|---|---|---|---|
| `D-S2-1` | Modelo jerárquico (`D0-1`): el Prestamista crea unilateralmente el `Contract` y a sus Deudores; el Deudor solo acepta términos | El Product Spec v2 describe un **marketplace**: el Deudor publica un "Loan Request" (dirección, monto, fotos, comps de RentCast) como `Public` (marketplace, identidad oculta) o `Private` (invita a un Prestamista puntual); un Prestamista responde emitiendo una Commitment Letter | `D0-1` (este documento), [08-contratos.md §8.1](08-contratos.md#81-ciclo-de-vida-y-estados) (asume que el `Contract` nace por acción del Lender) | **No se reemplaza `D0-1`.** Se agrega una etapa **anterior y opcional** al `Contract`: tabla nueva `LoanRequest` (Deudor-iniciada, `Public`/`Private`). Cuando un Prestamista "engancha" un `LoanRequest` (marketplace match o invitación privada aceptada), el servicio crea un `Contract(DRAFT)` igual que hoy (`BE-051`), con `Contract.loanRequestId` apuntando al origen — el resto del ciclo de vida (`ContractTerms`, aceptación bilateral D0-3, activación) **no cambia**. El flujo directo actual (Lender crea Contract sin `LoanRequest` previo) sigue existiendo para deals privados ya acordados fuera de la plataforma. **Confirmado con Spencer (2026-09-10)**: `ContractTerms` v1 se **pre-llena** con `totalLoanAmountRequested`/`requestedClosingDate` del `LoanRequest` al hacer match — punto de partida editable, no una oferta en blanco; el Prestamista ajusta tasa/plazo/fees antes de emitir la Commitment Letter | Evita reabrir D0-1 (ya confirmado explícitamente por Spencer) y evita duplicar la máquina de estados de `Contract`/`ContractTerms` ya construida — el marketplace es una capa de *discovery/matching* que desemboca en el mismo `Contract` de siempre, no un modelo de datos paralelo. El pre-llenado evita que el Prestamista reescriba a mano datos que el Deudor ya especificó al publicar el deal |
| `D-S2-2` | `ContractTerms` solo tiene `lateFeeType`/`lateFeeAmount` — ninguna otra estructura de fee | El Commitment Letter Spec exige una tabla de fees completa por contrato: origination points, processing fee, underwriting fee, doc prep fee, fees personalizados (nombre libre), pre-pay penalty, **más** el fee de plataforma (marketplace connection) — todos mostrados en una "Closing Fee Summary Table" dentro del mismo documento que el Deudor acepta | [04-base-de-datos.md §4.3](04-base-de-datos.md#contractterms-contracttermsacceptance-contractborrower) (`ContractTerms` no tiene estos campos) | Tabla nueva **`ContractFeeItem`** (N:1 con `ContractTerms`, no con `Contract` — los fees son parte de los términos que se versionan y aceptan, igual que la tasa o el plazo): `category` (`LENDER`/`PLATFORM`), `code` (enum cerrado + `CUSTOM`), `label` (nombre libre, obligatorio si `code=CUSTOM`), `amountType` (`FLAT`/`PERCENTAGE`), `amountValue`, `computedAmount` (snapshot en dólares, resuelto al crear la versión — un % de un monto que después cambia de versión no debe recalcularse solo). Se agrega `prePayPenaltyType`/`prePayPenaltyAmount` directo en `ContractTerms` (mismo patrón que `lateFeeType`/`lateFeeAmount`, ya existente) en vez de una fila de `ContractFeeItem`, porque no es un monto fijo en el momento de la firma — depende de cuándo se pre-pague | Sin esto, la Commitment Letter no tiene de dónde sacar su tabla de fees — es el requisito central del segundo documento. Separar `ContractFeeItem` de `ContractTerms` (en vez de columnas fijas `processingFee`/`underwritingFee`/...) es obligatorio por el propio requisito de "Custom Fees" (nombre y monto libres, cantidad no acotada) |
| `D-S2-3` | No existe ningún campo de suscripción del Deudor | "Borrower monthly subscription: $9/month — Recurring, billed monthly after 7-day trial" (ambos documentos coinciden) | — (hueco, no contradicción) | Tabla nueva **`BorrowerSubscription`** (1:1 con `BorrowerProfile`): `status` (`TRIALING`/`ACTIVE`/`PAST_DUE`/`CANCELLED`), `trialEndsAt`, `currentPeriodEnd`, `stripeSubscriptionId?`, `cancelledAt?`. Se crea automáticamente al activar la cuenta del Deudor (mismo punto que hoy dispara la contraseña temporal, `userActivation.service.ts`), en `TRIALING` con `trialEndsAt = now() + 7 días` | Resuelve parcialmente el riesgo #3 (antes "modelo de cobro de la plataforma: no se modela, alcance Q5 sin responder") — el lado Deudor del pricing **ya no es una pregunta abierta**, tiene número y cadencia concretos. El lado "cómo se cobra de verdad" (Stripe Billing/Invoicing) sigue bloqueado por `A-3` (Stripe Connect) — la tabla se modela, el cobro recurrente no se activa hasta esa decisión, mismo criterio ya usado con `Autopay` (`A-2`) |
| `D-S2-4` | `Transaction` no tiene ningún campo de fee de plataforma — ya señalado como hueco por el riesgo #2 ("¿desglosar `platformFeeAmount`/`stripeFeeAmount`?") | "ACH Transaction Fee: ACH cost + $9 / loan / month (max $99/mo)" — cobrado por Dueño a Dueño LLC sobre cada transacción mensual | Riesgo #2 de [15](15-riesgos-y-decisiones-pendientes.md) (antes abierto sin datos concretos) | Se agrega `Transaction.platformFeeAmount Decimal?` — calculado por el servicio de aplicación de pagos (`applyTransaction`, `BE-068`) como `stripeFee + 9.00`, con un tope acumulado de `$99/mes/contrato` verificado contra la suma de `platformFeeAmount` de transacciones `SUCCEEDED` del contrato en el mes en curso antes de aplicar el cargo completo | Cierra el riesgo #2 con un número concreto en vez de dejarlo como pregunta abierta — sigue bloqueado por `A-3` como el resto del módulo de pagos con Stripe, pero el campo ya se puede modelar |
| `D-S2-5` | `Contract` no distingue cómo se originó (siempre asumido: alta directa del Lender) | El fee de marketplace connection (1 pt, mín. $999) **solo** aplica si el match fue vía marketplace público; $0 si fue invitación privada o alta directa | — (consecuencia directa de `D-S2-1`) | `Contract` gana `originationSource` enum `ContractOriginationSource { DIRECT MARKETPLACE PRIVATE_INVITE }`, default `DIRECT` (preserva el comportamiento ya implementado de Fase 6 tal como está planeado). Cuando `originationSource = MARKETPLACE`, el servicio que genera **cualquier** versión de `ContractTerms` (v1 al hacer match, y también cada versión posterior creada por `BE-060`) inserta automáticamente un `ContractFeeItem(category=PLATFORM, code=MARKETPLACE_CONNECTION, computedAmount = MAX(principalAmount_de_esa_versión * 0.01, 999))`; en `PRIVATE_INVITE`/`DIRECT` no se inserta ninguna fila (fee = $0, ni siquiera una fila en $0, para que la Closing Fee Summary Table no la liste). **Confirmado con Spencer (2026-09-10)**: el fee **se recalcula en cada versión nueva** sobre el monto vigente de esa versión — no se congela en el valor del match original | Traduce literalmente la regla del Commitment Letter Spec ("Only applies to loans originated through the public marketplace. Private/invited deals: $0") a una regla de servicio determinista, sin dejarlo como texto libre que alguien tipee a mano en cada contrato. Recalcular en cada versión mantiene `MARKETPLACE_CONNECTION` consistente con el resto de `ContractFeeItem` (todos derivados del monto vigente de su propia versión, regla 16 de [04 §4.7](04-base-de-datos.md#reglas-de-negocio-nuevas-extiende-46)) — el Deudor ve el fee real de cada renegociación, no uno desactualizado |
| `D-S2-6` | No existe ningún concepto de verificación/vetting del Deudor | "Lender can require borrower to complete full application ($99 fee): entity documents, bank statement, past deal history, credit pull, background check, ID upload" | — (hueco) | Tabla nueva **`BorrowerApplication`**: `borrowerProfileId`, `lenderCompanyId` (quién la exige/revisa), `loanRequestId?` (contexto, si nació de un deal puntual), `status` (`REQUESTED`/`SUBMITTED`/`UNDER_REVIEW`/`APPROVED`/`REJECTED`), `feeAmount` (default `99.00`), `feePaidAt?`, `creditPullStatus?`/`creditPullExternalRef?`, `backgroundCheckStatus?`/`backgroundCheckExternalRef?`, `reviewedByUserId?`, `reviewedAt?`, `rejectionReason?`. Los documentos propios (entity docs, bank statement, ID) se adjuntan como filas de `Document` (`type=BORROWER_APPLICATION_DOCUMENT`, ver `D-S2-8`) enlazadas por `applicationId?` nuevo en `Document`. **No** se persiste el contenido crudo de un reporte de crédito/background check — solo el status y una referencia externa opaca del proveedor (mismo criterio ya aplicado a Stripe: nunca se guarda información sensible completa, sección [3.6](03-arquitectura-backend.md#36-seguridad-transversal)) | Es una entidad independiente del `Contract` (un Deudor se vetea una vez por Prestamista, no por cada contrato) y del `LoanRequest` (puede pedirse antes de que exista un deal concreto) — no cabía en ninguna tabla existente sin sobrecargarla |
| `D-S2-7` | `Property` (`D-P1-5`) ya tiene campos de valuation/ARV, pero **todos** de carga manual — riesgo #15 abierto ("¿fuente de datos: manual o API tipo RentCast?") | "RentCast API — pulls nearby rent rates and comparable sales (0.5mi, mismo sqft, últimos 6 meses + DOM). AI generates 3–4 rental comps y 3–4 sale comps automáticamente." El propio Product Spec v2 marca RentCast como `✅ Key on file` — a diferencia del resto de integraciones ("Needed"/"Evaluate") | Riesgo #15 de [15](15-riesgos-y-decisiones-pendientes.md) | **Resuelve parcialmente** el riesgo #15: la fuente confirmada es RentCast (mismo proveedor que ya usaba `Owner`, precedente directo en `app/api/analyze/route.ts`, ver [PAYMYLOAN_DATABASE_DESIGN.md §3](../PAYMYLOAN_DATABASE_DESIGN.md)). Se agregan a `Property`: `valuationSource` (`MANUAL`/`RENTCAST`), `rentCastFetchedAt?`, `estimatedRentAmount?`, `rentCompsSnapshot Json?`, `saleCompsSnapshot Json?`. **Se decide explícitamente no** crear tablas relacionales `PropertyRentComp`/`PropertySaleComp`: los comps son una foto de underwriting en el momento del listing, no algo que se filtre/consulte de forma independiente — un snapshot `Json` evita modelar el 100% del payload de un proveedor externo que puede cambiar de forma sin aviso. Servicio nuevo `fetchRentCastComps(propertyId)`, aislado en `src/services/rentcast.service.ts` para poder sustituirlo si el proveedor cambia | El campo `estimatedMarketValue`/`afterRepairValue` (carga manual, `D-P1-5`) **no se reemplaza** — RentCast informa renta/comps de venta, no un ARV definitivo; el Prestamista sigue pudiendo sobreescribir a mano. La API key ya está disponible según el propio documento, así que a diferencia de crédito/background/e-signature esto **no** es un riesgo bloqueante, es trabajo de implementación normal |
| `D-S2-8` | `Document.contractId` es una FK **obligatoria** — todo documento cuelga de un `Contract` que ya existe | Las fotos del `LoanRequest` (front, kitchen, bathrooms, yard, roof, etc.) se suben **antes** de que exista ningún `Contract` — son parte del listing, no del préstamo activo | [04-base-de-datos.md §4.3](04-base-de-datos.md#scheduledpayment-transactionallocation-webhookevent-refreshtoken-passwordresettoken-twofactorrecoverycode-document) (`Document.loanId`/`contractId` obligatorio) | `Document.contractId` pasa a **opcional**; se agrega `Document.loanRequestId?` y `Document.applicationId?` (para `D-S2-6`) — exactamente uno de los tres (`contractId`/`loanRequestId`/`applicationId`) debe estar presente, verificado a nivel de servicio (Postgres no expresa "exactamente uno de N" nativamente sin un `CHECK` multi-columna, que sí se puede agregar como `@@check` manual en la migración — se evalúa al implementar, no bloquea el diseño). Se agregan `DocumentType.LOAN_REQUEST_PHOTO`, `BORROWER_APPLICATION_DOCUMENT`, `COMMITMENT_LETTER`, `PAYOFF_LETTER`, `EXTRA_DISCLOSURE` al enum existente | Reutiliza la infraestructura de `Document` ya diseñada (S3 privado, URL firmada de corta vida, nunca URL pública — sección [5.10 de PAYMYLOAN_DATABASE_DESIGN.md](../PAYMYLOAN_DATABASE_DESIGN.md), ya adoptada) en vez de crear un segundo gestor de archivos solo para fotos de listing |
| `D-S2-9` | La aceptación de `ContractTerms` (D0-3) ya es vinculante y auditada, pero no existe ningún artefacto PDF ni distribución automática | "Once signed by both parties, it auto-routes to the closing attorney and insurance company... System auto-generates PDF with all fields + fee table" | `IMPLEMENTATION_PROGRESS.md` Fase 11, `PB-003` ("Commitment letter — la aceptación en plataforma es la firma"), ya anticipado pero sin ningún detalle de distribución | **No se crea una tabla `CommitmentLetter`.** La Commitment Letter **es** el PDF generado de una `ContractTerms` ya `ACCEPTED` (D0-3) + sus `ContractFeeItem` (`D-S2-2`) — se persiste como una fila de `Document` (`type=COMMITMENT_LETTER`, `contractId` presente). Se agregan a esa fila `sentToTitleCompanyAt?`/`sentToInsuranceCompanyAt?` (en vez de una tabla de distribución aparte — son solo 2 hitos, no un log recurrente). El disparador es el mismo punto que ya activa el contrato (`generateAmortizationSchedule`, `BE-059`, al completar el quórum de aceptación) — se le agrega, en la misma operación: generar el PDF y encolar los 2 correos | La aceptación bilateral ya es el "acto de firma" (decisión `D0-3`, ya implementada como principio de diseño desde el inicio del plan) — la Commitment Letter es su *representación documental*, no un segundo flujo de aprobación paralelo. Evita duplicar estado (una tabla `CommitmentLetter.status` que tendría que mantenerse sincronizada a mano con `ContractTerms.status`) |
| `D-S2-10` | `BorrowerProfile` no tiene ningún dato de abogado de cierre ni aseguradora "on file" | "Borrower Profile: Closing attorney on file · Insurance company on file" | [04-base-de-datos.md §4.3](04-base-de-datos.md#borrowerprofile-existente--pasa-a-ser-a-nivel-de-plataforma) | Se agregan a `BorrowerProfile`: `closingAttorneyName?`/`closingAttorneyEmail?`/`closingAttorneyPhone?`. La aseguradora "on file" del Deudor **no** duplica `InsuranceCompanyProfile` (directorio de plataforma, `D-P1-7`) — se modela como `BorrowerProfile.defaultInsuranceCompanyId? → InsuranceCompanyProfile`, un valor por default que se copia a `Contract.insuranceCompanyId` al crear el contrato pero sigue siendo editable por contrato (mismo patrón default-copiable que ya existe implícitamente para el abogado). `Contract` gana `closingAttorneyName?`/`closingAttorneyEmail?`/`closingAttorneyPhone?` propios — snapshot editable en el momento de emitir la Commitment Letter, no una referencia viva al perfil (si el Deudor cambia de abogado después, no debe reescribir un documento ya distribuido) | El "on file" del Deudor es un *default operativo* (llenar el formulario más rápido), no la fuente de verdad legal de un contrato ya firmado — de ahí el snapshot en `Contract` en vez de una FK viva |
| `D-S2-11` | `LenderCompany` no tiene preferencias de rate/term/tipo de préstamo, capital disponible/desplegado, ni áreas geográficas | "Lender Profile: Rate/term/loan type preferences · Available capital to deploy · Geographic areas/loan types preferred" — y el Lender Dashboard pide mostrar "Available Capital / Deployed Capital" | [04-base-de-datos.md §4.3](04-base-de-datos.md#lendercompany-nueva--es-el-tenant-real) | Se agregan a `LenderCompany`: `ratePreferenceMinPercent?`/`ratePreferenceMaxPercent?`, `loanTypePreferences LoanStructure[]?`, `geographicAreaStates String[]?` (códigos de estado, simple — no se modela un radio geográfico real), `availableCapital Decimal?` (carga manual del Lender, igual que `estimatedMarketValue` de `Property` — sin integración bancaria que lo verifique). `deployedCapital` **no** es una columna — es derivado (`SUM(Contract.currentPrincipalBalance) WHERE lenderCompanyId=... AND status IN (ACTIVE, DELINQUENT)`), mismo criterio que `Contract.currentPrincipalBalance` (`D7` de [PAYMYLOAN_DATABASE_DESIGN.md](../PAYMYLOAN_DATABASE_DESIGN.md) — caché derivado, nunca fuente de verdad editable a mano) | Evita inventar una columna que se desincronice del saldo real de los contratos — el capital desplegado *es* la suma de lo que ya está prestado, no un número aparte que alguien deba actualizar manualmente |
| `D-S2-12` | No existe ninguna tabla de notificaciones in-app; los dashboards de ambos documentos piden listas de notificaciones ("payments made, lender inquiries, alerts" / "payments received, payoff requests") | Requisito explícito de ambos dashboards | — (hueco) | Tabla nueva **`Notification`**: `userId`, `type` (`String` libre + union de TypeScript, mismo criterio ya usado en `AuditLog.action` — sección [5.12 de PAYMYLOAN_DATABASE_DESIGN.md](../PAYMYLOAN_DATABASE_DESIGN.md)), `title`, `body`, `isRead Boolean @default(false)`, `readAt?`, `relatedEntityType?`/`relatedEntityId?`, `createdAt`. El envío por email/SMS lo dispara el mismo evento de servicio que crea la fila (ej. `applyTransaction` crea `Notification` + llama `sendEmail`) — **no** se modela una tabla de intentos de entrega por canal en esta fase (sería sobre-ingeniería sin un proveedor de SMS confirmado, ver riesgo #25 en [15](15-riesgos-y-decisiones-pendientes.md)) | Reutiliza el mismo principio ya aplicado a `AuditLog.action` (enum de Postgres obligaría a migración por cada tipo de evento nuevo) para un problema estructuralmente idéntico |
| `D-S2-13` | `IMPLEMENTATION_PROGRESS.md` (Fase 13, `PB-009`/`PB-010`) ya planea "rating PML" del Deudor y "stats" del Prestamista — pero como **métricas calculadas** desde el historial de pagos/contratos, no como reseñas enviadas por un usuario | "Lender ratings — borrowers can rate lenders" (Product Spec v2, sección "Other Features") — es explícitamente una **reseña enviada por el Deudor**, no un cálculo | `PB-009`/`PB-010` (ya planeados, siguen sin cambios) vs. este requisito nuevo, que es una entidad distinta | Tabla nueva **`LenderReview`**: `lenderCompanyId`, `borrowerProfileId`, `contractId` (contexto — de qué préstamo completado/activo habla la reseña), `rating Int` (1–5), `comment?`, `createdAt`. `@@unique([contractId, borrowerProfileId])` — una reseña por Deudor por contrato, no acumulable infinitamente sobre el mismo préstamo. `LenderCompany.averageRating` se deriva (`AVG(LenderReview.rating)`), mismo criterio que `deployedCapital` (`D-S2-11`) | No se fusiona con `PB-009`/`PB-010` porque son conceptualmente distintos: un "borrowing score" calculado no tiene texto libre ni es algo que el Deudor "envía"; una reseña sí. Ambos pueden coexistir y mostrarse juntos en el perfil público del Prestamista |
| `D-S2-14` | [02-diferencias-owner.md](02-diferencias-owner.md) decidió explícitamente que **no** hay invitación de Deudor por correo con auto-creación de cuenta — el Deudor lo crea el Prestamista directo (`BE-045`, ya implementado) | "Invite system — borrowers can invite lenders via portal link (and vice versa)" | [02-diferencias-owner.md](02-diferencias-owner.md) fila `VerificationToken`/invitación | **No se reabre** el flujo ya implementado de Fase 5 (`BE-045`, Lender crea Borrower directo) — sigue igual. Se agrega un mecanismo **nuevo y acotado**, exclusivo del contexto `LoanRequest` (`D-S2-1`): invitar a una contraparte específica (con o sin cuenta todavía) a un `LoanRequest` puntual. Reutiliza el *patrón* ya validado de token único + expiración (mismo que `PasswordResetToken`) en una tabla ligera `LoanRequestInvite` (`loanRequestId`, `invitedEmail`, `token` único, `expiresAt`, `acceptedAt?`, `acceptedByUserId?`) — si el invitado no tiene cuenta, aceptar la invitación lo manda al flujo de auto-registro ya existente (`D-P2-1`) con el `LoanRequest` pre-vinculado | Cierra el requisito real (invitar a alguien a un deal puntual, con o sin cuenta) sin reabrir la decisión ya tomada de que el alta general de un Deudor por un Lender no usa invitación por correo — son dos casos de uso distintos (dar de alta a "mi deudor" vs. invitar a un préstamo específico) |
| `D-S2-15` | `LoanStructure { INTEREST_ONLY AMORTIZED BALLOON }` | El Product Spec v2 lista "Construction Draw Loan" explícitamente marcado **`FUTURE FEATURE`** en su propia tabla de tipos de préstamo | — | **No se agrega** ningún valor nuevo a `LoanStructure` esta ronda. "Interest Only (Short Term)" e "Interest Only (Long Term)" del spec son ambos `INTEREST_ONLY` con distinta `maturityDate` (ya representable); "Fully Amortized" a 5/7/10/15/20/25/30 años es `AMORTIZED` con `amortizationTermMonths` (ya representable) | El propio documento fuente marca Construction Draw como fuera de esta ronda — no hay motivo para adelantar el enum |
| `D-S2-16` | — | "Outreach — text/email campaigns targeting slow flip borrowers nationally" | — | **Fuera de alcance de este backend.** Es una función de marketing/growth (campañas salientes a una audiencia externa a la plataforma, no notificaciones a usuarios existentes) — no se modela ninguna tabla ni endpoint. Se deja registrado aquí para que no se pierda como requisito del documento fuente, no porque el plan lo vaya a construir | Ningún endpoint ni flujo del documento lo describe con el mismo nivel de detalle que el resto — es la única sección de "Other Features" sin contraparte técnica clara, consistente con ser una actividad operativa (herramienta de email marketing de terceros), no una feature de producto |
| `D-S2-17` | — | Ambos documentos exigen lenguaje de disclaimer específico (Dueño a Dueño LLC no es lender/broker), divulgación de fees en momentos concretos (aplicación antes de enviarla, connection fee antes de aceptar la Commitment Letter) | — | **No requiere schema nuevo.** El orden de disclosure ya queda satisfecho estructuralmente por el diseño existente: los `ContractFeeItem` (`D-S2-2`) viven en la versión de `ContractTerms` que el Deudor ve **antes** de aceptar (D0-3); el fee de aplicación (`D-S2-6`) se muestra antes de que el Deudor la envíe (regla de UI/frontend, no de datos). El texto legal en sí (disclaimer, T&C) es contenido estático servido por el frontend o un documento versionado fuera de este backend — **pendiente de abogado**, según el propio documento fuente | No hay ninguna tabla o endpoint que este documento deba crear para esto — es un recordatorio de que el *momento* en que cada fee se muestra ya está resuelto por el orden natural del flujo, no una tarea de ingeniería nueva |
| `D-S2-18` | `LenderCompany` no tiene ningún campo de cuenta bancaria propia — solo `stripeConnectedAccountId` (para *recibir* pagos ACH del Deudor vía Stripe, una vez resuelta `A-3`) | Tanto la Commitment Letter como la Payoff Letter deben mostrar "lender bank info (last 4 only) + phone number to verify before wire" — la cuenta donde el abogado de cierre debe wirear los fees del Prestamista, un dato **distinto** de la cuenta ACH de Stripe (el wire de cierre es una transferencia manual bancaria, no un cobro ACH recurrente) | [04 §4.3](04-base-de-datos.md#lendercompany-nueva--es-el-tenant-real) (`LenderCompany` sin este campo) | Se agregan a `LenderCompany`: `wireBankAccountLast4` (String?, 4 dígitos), `wireVerificationPhone` (String?) — deliberadamente **no** se guarda el número de cuenta completo ni el routing number (mismo principio ya aplicado a `Transaction.last4`, sección [3.6](03-arquitectura-backend.md#36-seguridad-transversal): nunca se persiste información bancaria completa). Ambos campos son de carga manual por el Prestamista, editables vía `PATCH /api/admin/lenders/:id/companies/:companyId` (`D-P4-8`, ya implementado — solo gana 2 campos más al schema) | Cierra un hueco real: sin esto, ni la Commitment Letter (`PB-003`) ni la Payoff Letter (`PB-007`) pueden mostrar el dato que el propio documento fuente marca como obligatorio en ambos flujos |

---

## Correcciones 2026-09-10 (autorrevisión post-commit, antes de implementar)

Al verificar que Fase 0–5 (ya implementada y comiteada, `fb1023e`) seguía sana bajo el plan actualizado, aparecieron dos huecos propios de la ronda anterior — ninguno afecta código ya construido, ambos se corrigen aquí antes de que alguien empiece Fase 6/13.

| # | Comportamiento actual | Problema encontrado | Decisión | Motivo |
|---|---|---|---|---|
| `D-S2-19` | `04 §4.7` listaba `Contract.originationSource`/`Contract.loanRequestId` como "campos nuevos" sin decir en qué migración entran; `14-roadmap.md` afirmaba explícitamente "Fase 13 no bloquea Fase 6" | `Contract.loanRequestId` es una FK a `LoanRequest`, tabla que no existe hasta Fase 13 — no se puede crear esa FK en la migración de Fase 6 sin que `LoanRequest` ya exista. Las dos afirmaciones (Fase 13 no bloquea Fase 6, y Fase 6 necesita esas columnas) son incompatibles entre sí | **`Contract.originationSource`/`Contract.loanRequestId` se agregan en la migración de Fase 13 (`PB-017`), no en la de Fase 6.** Fase 6 (`BE-051`) construye `Contract`/`ContractTerms`/`ContractFeeItem` completos sin ellas — todo contrato que nace en Fase 6 es implícitamente el equivalente a `DIRECT`, sin necesidad de que la columna exista todavía. `ContractFeeCategory`/`ContractFeeCode` (enums, sin FK) sí se crean en Fase 6 con el valor `MARKETPLACE_CONNECTION` ya presente, aunque nada lo use hasta Fase 13 — un enum no tiene el problema de dependencia que sí tiene una FK. `PB-017` (Fase 13) hace el `ALTER TABLE contracts ADD COLUMN "originationSource" ..., ADD COLUMN "loanRequestId" ...` sobre la tabla que Fase 6 ya dejó lista, y **amplía** el servicio de `BE-060` (proponer nueva versión de términos, Fase 6) para que recién ahí empiece a verificar `originationSource=MARKETPLACE` y recalcular `MARKETPLACE_CONNECTION` — ese `if` no puede existir en el código de Fase 6 porque la columna que consulta no existe todavía | Deja a Fase 6 completamente autocontenida (se puede implementar y dar por terminada sin que Fase 13 exista) y evita prometer una FK contra una tabla que nace tres fases después — mismo criterio de secuenciación ya aplicado en todo el resto del plan (ej. `Property` antes que `Contract`, `10.1`) |
| `D-S2-20` | `PB-017` (match de marketplace) no especificaba cómo se resuelve la `LenderCompany` destino cuando el Prestamista que hace match tiene más de una | El resto del plan ya tiene una regla establecida para esto exacto (`BE-045`/`D-P4-1`: con una sola `LenderCompany` se resuelve sola, con más de una exige `lenderCompanyId` explícito en el body, validado contra las propias, `404` si no es una de las suyas) — `PB-017` la omitió | `POST /api/marketplace/loan-requests/:id/match` sigue exactamente la misma regla que `BE-045`: `lenderCompanyId` opcional si el Lender tiene una sola `LenderCompany` (se resuelve sola), obligatorio si tiene más de una (`400 LENDER_COMPANY_REQUIRED`, mismo código de error ya existente), `404` si el `lenderCompanyId` enviado no es propio | Reutiliza un patrón ya implementado y probado (`BE-045`, Fase 5, en producción) en vez de inventar uno nuevo para el mismo problema exacto — un Lender con N empresas no tiene forma de que el servicio adivine con cuál está operando, sea para crear un Borrower o para hacer match de un deal |

---

**Nuevos pendientes que este round añade a la sección [15](15-riesgos-y-decisiones-pendientes.md)** (no resueltos aquí — proveedor de crédito/background check/e-signature/OTP-SMS sin confirmar, "Deal analyzer API" en evaluación, fee de almacenamiento de archivos `TBD`, si `CPA`/`Title Company` necesitan cuenta propia sigue abierto per `D-P1-1`): ver tabla actualizada en [15](15-riesgos-y-decisiones-pendientes.md#nuevos-pendientes--ronda-2026-09-10-ver-00-sección-decisiones-2026-09-10).

**Impacto en el roadmap**: ver [13. Backlog](13-backlog.md) y [14. Roadmap](14-roadmap.md) — se formalizan como fases completas (con tickets `PB-0xx`) las fases 11 (Documentos/Commitment Letter/Notificaciones), 12 (Payoff), 13 (Marketplace/Loan Requests/Vetting), 14 (Dashboards, nueva), 15 (Exports/Reporting), que antes solo existían como una fila suelta en [IMPLEMENTATION_PROGRESS.md](../IMPLEMENTATION_PROGRESS.md) sin ningún documento de plan detrás — mismo defecto que ya se corrigió una vez para Fase 1 (`C-1`/`M-1` nunca integrados) y que esta ronda cierra para no repetirlo.

---

[← Índice del plan](README.md)  ·  [Siguiente: 1. Resumen de arquitectura propuesta](01-resumen-arquitectura.md)
